# CSRF protection configuration
# The form component uses session-based CSRF (generated server-side in HTML).
# Only login/logout use stateless CSRF (cookie-based) as required by Symfony security.
framework:
    csrf_protection:
        stateless_token_ids:
            - authenticate
            - logout
